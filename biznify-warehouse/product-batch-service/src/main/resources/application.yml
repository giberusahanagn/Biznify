spring:
  application:
    name: product-batch-service

  datasource:
    url: jdbc:mysql://localhost:3306/biznify_product_batch_service
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: sahanagn275@gmail.com
    password: fypt xatx qwap dlsi
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

server:
  port: 8084
